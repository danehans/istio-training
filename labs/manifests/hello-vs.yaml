apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
    name: hello
spec:
    hosts:
      - "*"
    gateways:
      - hello
    http:
      - route:
          - destination:
              host: hello-world.default.svc.cluster.local
              port:
                number: 80
